#pragma once

namespace Constants {
    // ASCII offset
    const static int OFFSET_A = 65;
    // Corresponds to number of queens. max N is 61
    const static int N = 21;
    // A goal h, which is for HC
    const static int GOAL_H = 0;
    // This sets the size of the population for GA
    const static int POPULATION_SIZE = 100;
    // This sets a limit on the main loops of the algorithms
    const static int COUNTER = 4000;
};